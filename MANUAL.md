# 📘 MUDMUE Web — คู่มือการใช้งาน

คู่มือ "เมนูนี้ทำอะไร ใช้ยังไง" ของทุกแอปในเว็บนี้ — เขียนไว้อ่านตอนลืม

> อยากรู้ว่าโค้ดอยู่ไฟล์ไหน ดูที่ [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) แทน
> ไฟล์นี้พูดถึง **การใช้งาน** อย่างเดียว

---

## สารบัญ

| # | แอป | ทำอะไร | URL |
|---|---|---|---|
| 0 | [หน้าแรก & การนำทาง](#0-หน้าแรก--การนำทาง) | ฮับรวมแอป | `/` |
| 1 | [🏸 MUDMUE Chok](#1--mudmue-chok--จับคู่ผู้เล่นแบด) | สุ่มจับคู่แบด + นับคะแนน + เก็บสถิติ | `/chok` |
| 2 | [🧮 MUDMUE Han](#2--mudmue-han--หารค่าแบด) | หารค่าคอร์ท/ค่าลูกหลังเล่นจบ | `/han` |
| 3 | [⚔️ WuWa MudMue](#3-️-wuwa-mudmue--ban--pick) | กระดาน Ban & Pick ทัวร์นาเมนต์ Wuthering Waves | `/ww-draft` |
| 4 | [🗺️ MUDMUE Chim](#4-️-mudmue-chim--แผนที่ร้านอาหาร) | ปักหมุด + รีวิวร้านอาหาร | `/chim` |
| 5 | [📖 Story Book](#5--story-book) | คลัง component ของเว็บ | `/story-book` |
| — | [ข้อมูลเก็บที่ไหน](#-ข้อมูลเก็บที่ไหน) | localStorage keys ทั้งหมด | — |
| — | [ยังไม่ได้ทำ / phase 2](#-ยังไม่ได้ทำ--รอ-phase-2) | ของที่ตั้งใจข้ามไว้ | — |

---

## 0. หน้าแรก & การนำทาง

| จุด | ทำอะไร |
|---|---|
| **โลโก้มุมซ้ายบน** | กดแล้วกลับหน้าแรกได้ทุกหน้า |
| **ปุ่มแฮมเบอร์เกอร์มุมขวาบน** | เปิดเมนูหลัก (drawer) — Home / Chok / Chim / Han / WuWa / Story Book |
| **การ์ดบนหน้าแรก** | ทางลัดเข้าแต่ละแอป |
| **Breadcrumb ใต้หัวข้อ** | บอกว่าอยู่หน้าไหน กดย้อนได้ |
| **แท็บของแต่ละแอป** | จอกว้าง = แถบด้านซ้าย · จอแคบ = แถวแท็บด้านบน (เนื้อหาเหมือนกัน) |

> URL จริงมี base path `/MUDMUE` นำหน้าเสมอ เช่น `/MUDMUE/han/calculator`
> ลิงก์เก่า `/pick` จะเด้งไป `/chok` ให้อัตโนมัติ

---

## 1. 🏸 MUDMUE Chok — จับคู่ผู้เล่นแบด

4 แท็บ: **Matchmaker → Dashboard → History** คือเส้นทางหลัก ส่วน **Profile** คือฐานข้อมูลคน

### 1.1 แท็บ Matchmaker — สุ่มจับคู่

ใช้ตอนจะเริ่มตี: ตั้งค่า → เลือกคน → กด Random → ยืนยัน → แมตช์ไปโผล่ที่ Dashboard

**ตัวเลือกฝั่งซ้าย**

| ตัวเลือก | ความหมาย |
|---|---|
| โหมดจับคู่ | ตอนนี้มีแค่ **สุ่ม (Random)** · โหมด "ตามฝีมือ" ยังปิดอยู่ (เร็ว ๆ นี้) |
| จำนวนผู้เล่นต่อแมตช์ | **2** (เดี่ยว) หรือ **4** (คู่) |
| จำนวนรอบ | สุ่มกี่แมตช์รวดเดียว |
| ☑ ข้ามอนิเมชันรูเล็ต | ไม่ต้องรอวงล้อหมุน · **ถ้าจำนวนรอบ > 1 จะถูกบังคับติ๊กและกดแก้ไม่ได้** |
| ☑ ซ่อนคนที่สุ่มได้ออกจากลิสต์ | พอปิดหน้าต่างผล คนที่เพิ่งถูกสุ่มจะถูกซ่อน ไม่โดนสุ่มซ้ำในรอบถัดไป |

**การจัดการรายชื่อ**

- **เพิ่มผู้เล่นชั่วคราว** — พิมพ์ชื่อแล้วกดเพิ่ม ใช้ได้เฉพาะรอบนี้ ⚠️ **ไม่ถูกบันทึกเข้า Profile** สถิติแพ้/ชนะจึงไม่ขึ้น
- **เลือกผู้เล่นจาก Profile** — ดึงคนที่มีโปรไฟล์อยู่แล้วเข้าลิสต์ (มีปุ่มเลือกทั้งหมด) คนที่อยู่ในลิสต์แล้วจะเลือกซ้ำไม่ได้
- **ปุ่มซ่อนรายคน** — พักคนนั้นออกจากการสุ่มโดยไม่ต้องลบ
- **ปุ่มลบ (✕) รายคน** — เอาออกจากลิสต์รอบนี้
- **ปุ่มรีเซ็ต** — คืนลิสต์เป็นค่าเริ่มต้น

> 💾 ลิสต์ผู้เล่นเก็บใน **sessionStorage** — ปิดแท็บเบราว์เซอร์แล้วหาย (ต่างจากที่อื่นในเว็บที่เก็บถาวร)

**การสุ่มหลายรอบทำงานยังไง**

สุ่มแบบ pool + carry-over: หยิบคนจาก pool ทีละรอบจนไม่พอ คนที่เหลือ (ไม่ครบทีม) จะถูก **ยกไปเป็นคนแรกของรอบถัดไป** แล้วค่อยเติมคนใหม่จาก pool ที่สับใหม่ — ทุกคนจึงได้ลงสนามเฉลี่ยกัน ไม่ใช่สุ่มอิสระทีละรอบ

**หน้าต่างผลการสุ่ม**

- ครึ่งแรกของแต่ละกลุ่ม = ทีม **แดง** · ครึ่งหลัง = ทีม **น้ำเงิน**
- **ฝั่งเสิร์ฟถูกสุ่มให้อัตโนมัติ**
- กด **ยืนยัน** แมตช์ทั้งหมดจะไปโผล่ที่ Dashboard · กดปิดเฉย ๆ = ทิ้งผลนั้น

**ปุ่ม Random กดไม่ได้เมื่อ:** คนที่พร้อมสุ่ม (ไม่ถูกซ่อน) น้อยกว่าจำนวนผู้เล่นต่อแมตช์ — จะมีข้อความบอกว่าตอนนี้มีกี่คน

### 1.2 แท็บ Dashboard — แมตช์ที่กำลังแข่ง

แสดงเฉพาะแมตช์ที่ **ยังไม่จบ** (ใหม่สุดอยู่บน)

- **ปุ่ม +/− บนการ์ด** — นับคะแนนแต่ละทีม
- **ตำแหน่งยืนขยับเองตามกติกาแบดจริง:**
  - ทีมที่ **กำลังเสิร์ฟ** ได้แต้ม → คนในทีมนั้น **สลับช่องซ้าย-ขวา** (ฝั่งเสิร์ฟไม่เปลี่ยน)
  - ทีมที่ **ไม่ได้เสิร์ฟ** ได้แต้ม → **เปลี่ยนฝั่งเสิร์ฟ** ไปเป็นทีมนั้น
  - เดี่ยว: คะแนนคู่ = ยืนช่องขวาของตัวเอง · คี่ = ช่องซ้าย และผู้รับยืนทแยงมุมเสมอ
- **ปุ่มจบแมตช์** — ตัดสินผู้ชนะจากคะแนน (เท่ากัน = เสมอ) แล้ว:
  1. ย้ายแมตช์ไปแท็บ History
  2. บวก win/lose ให้ทุกคนที่มี Profile — **ยกเว้นแมตช์ที่เสมอ ไม่นับให้ใครทั้งคู่**

> ⚠️ **คะแนนระหว่างแข่งยังไม่ถูกบันทึกลงเครื่อง** — เก็บอยู่ในหน่วยความจำของหน้าเท่านั้น
> ถ้ารีเฟรชหรือสลับแท็บออกไปก่อนกด "จบแมตช์" คะแนนจะกลับไปเป็น 0

### 1.3 แท็บ History — ประวัติแมตช์

- แมตช์ที่จบแล้ว เรียงจากที่จบล่าสุด
- แต่ละฝั่งมีป้าย **WIN / LOSE / DRAW**
- **ปุ่มแข่งใหม่** — สร้างแมตช์ใหม่ด้วยผู้เล่นชุดเดิม คะแนนเริ่มที่ 0 สุ่มฝั่งเสิร์ฟใหม่ แล้วไปรอที่ Dashboard (มี toast แจ้ง)

### 1.4 แท็บ Profile — ฐานข้อมูลผู้เล่น

**นี่คือรายชื่อกลางของทั้งเว็บ** — MUDMUE Han ใช้ลิสต์ก้อนเดียวกันนี้

| คอลัมน์ | ความหมาย |
|---|---|
| ชื่อ | ชื่อที่แสดง (ถ้าไม่ได้ตั้ง จะใช้ชื่อจริง) |
| Level | ระดับมือ |
| Win/Lose | สะสมจากการกด "จบแมตช์" ที่ Dashboard |
| W/L Ratio | win ÷ lose · แสดง `-` ถ้ายังไม่เคยแพ้ · `0` ถ้ายังไม่เคยชนะ |

**ระดับมือ เรียงจากอ่อน → เก่ง:** `BG · N · S · P · C · B · A`

**เพิ่ม/แก้ไขโปรไฟล์** — มี 3 ช่อง: ชื่อจริง / ชื่อที่แสดงในเว็บ / Level

**การลบ**

ถ้าคนนั้น **ติดอยู่ในบิลของ MUDMUE Han** ระบบจะบอกจำนวนบิล และเสนอให้ **"ซ่อน" แทนการลบ**

- **ซ่อน (archive)** = หายจากลิสต์ "เลือกคน" ทุกที่ แต่บิล/ประวัติเก่ายังอ่านชื่อได้ปกติ · ในตารางจะขึ้นป้าย "ซ่อนอยู่"
- **ลบจริง** = หายถาวร ประวัติเก่าที่อ้างถึงจะเหลือแค่ชื่อที่ถูกคัดลอกเก็บไว้ตอนบันทึก

---

## 2. 🧮 MUDMUE Han — หารค่าแบด

3 แท็บ: **คำนวณ / ประวัติ / ตั้งค่า**

### 2.1 แท็บ คำนวณ

เรียงจากบนลงล่างตามลำดับที่เกิดจริงหน้างาน — กรอกไล่ลงมาได้เลย

#### ① หัวบิล

ชื่อก๊วน (เช่น "แบดวันเสาร์") + วันที่เล่น — กรอกย้อนหลังได้

#### ② ค่าคอร์ท

แนวคิด: **"ป้ายราคา" เป็นของแยกชิ้นจาก "คอร์ท"** สร้างป้ายไว้ก่อน แล้วเอาไปแปะบนคอร์ทที่ใช้ราคานั้นจริง

**การ์ดป้ายราคา (ด้านล่าง)** — แต่ละใบมี ชื่อช่วง / ราคาต่อ ชม. / จำนวน ชม.

- กด **"+ เพิ่มช่วงราคา"** เพื่อสร้างใบใหม่
- ชั่วโมงปรับทีละ 0.5 ได้ (1.5 ชม. เป็นเรื่องปกติ)
- ถ้าชั่วโมง ≠ 1 การ์ดจะโชว์ยอดคูณให้ดู เช่น `= ฿300`
- ป้ายที่ถูกใช้อยู่จะมีป้ายเล็กบอก **"n จุด"** = ถูกแปะอยู่กี่ที่

**วิธีแปะป้ายลงคอร์ท — ทำได้ 2 ทาง**

1. **ลาก** การ์ดป้ายไปวางบนการ์ดคอร์ท (เมาส์)
2. **แตะการ์ดป้าย 1 ที → แตะคอร์ท** (มือถือใช้ทางนี้ เพราะลากบนทัชไม่ทำงาน) — ป้ายที่เลือกอยู่จะเปลี่ยนสี และมีข้อความบอกด้านบน

**การ์ดคอร์ท (ด้านบน)** — โชว์ป้ายที่แปะอยู่, **ชั่วโมงรวมของคอร์ทนั้น**, และยอดเงินของคอร์ทนั้น

- แตะที่ชิปป้ายบนคอร์ท = เอาป้ายใบนั้นออกจากคอร์ทนี้
- ปุ่ม ✕ มุมการ์ด = ลบคอร์ท (ต้องเหลืออย่างน้อย 1 คอร์ทเสมอ)
- **ป้ายใบเดียวแปะได้หลายคอร์ท** (หลายคอร์ทเล่นชั่วโมงเดียวกัน ราคาเดียวกัน)
- **คอร์ทเดียวแปะได้หลายป้าย** (เล่นข้ามชั่วโมงที่ราคาต่างกัน) และแปะป้ายเดิมซ้ำได้ (เล่น 2 ชม. ราคาเดียวกัน)
- ⚠️ ลบป้ายต้นฉบับที่การ์ดด้านล่าง = ป้ายนั้นหลุดออกจาก **ทุก** คอร์ทที่แปะไว้ด้วย

#### ③ ค่าลูกแบด

ราคาต่อลูก + จำนวนลูกที่ใช้ไปทั้งวัน (กรอกหลังเล่นจบเท่านั้น เพราะต้องนับลูกจริง)

#### ④ คนมาวันนี้

- แตะชื่อเพื่อเลือก/ไม่เลือก
- ช่อง **"เพิ่มชื่อใหม่"** — ชื่อที่เพิ่มที่นี่จะ **เข้าไปอยู่ใน Profile ของ MUDMUE Chok ด้วย** (รายชื่อก้อนเดียวกัน) ถ้าชื่อซ้ำกับที่มีอยู่ จะใช้คนเดิมไม่สร้างซ้ำ
- คนที่ถูก "ซ่อน" ไว้ที่หน้า Profile จะมีป้ายกำกับ

#### ⑤ ปรับจำนวนลูกเฉพาะคน

- ทุกคนเริ่มที่ **"ค่าเริ่มต้น · N ลูก"** = ร่วมหารครบทุกลูกที่ใช้ทั้งวัน
- กดปุ่มค่าเริ่มต้น 1 ที ถึงจะเปิดช่องให้พิมพ์ (กันเผลอพิมพ์ทับ) · กด ↺ เพื่อคืนค่าเริ่มต้น
- ใช้ตอนมีคนตีลูกน้อยกว่าคนอื่นจริง ๆ — ที่เหลือจะไปเฉลี่ยกันเองอัตโนมัติ
- ไม่ปรับก็ได้ ข้ามไปดูสรุปได้เลย

#### ⑥ สรุปผล

ตารางต่อคน + ยอดรวม + ปุ่ม 3 ปุ่ม

| ปุ่ม | ทำอะไร |
|---|---|
| **บันทึกลงประวัติ** | เก็บบิลนี้เข้าแท็บประวัติ · กดซ้ำหลังแก้ยอด = บันทึกทับใบเดิม |
| **QR รับเงิน** | เปิดรูป QR พร้อมเพย์ที่ตั้งไว้ (ต้องอัปโหลดที่แท็บตั้งค่าก่อน) — QR ยังไม่ฝังจำนวนเงิน ต้องพิมพ์ยอดเอง |
| **เริ่มบิลใหม่** | ล้างบิลทั้งใบ (ถามยืนยันก่อน และเตือนแรงขึ้นถ้ายังไม่ได้บันทึก) |

### 🧾 สูตรคำนวณของ Han

```
ยอดของป้ายราคา 1 ใบ = ราคาต่อ ชม. × จำนวน ชม.
ยอดของคอร์ท 1 คอร์ท = Σ ยอดของทุกป้ายที่แปะบนคอร์ทนั้น
ค่าคอร์ทรวม         = Σ ยอดของทุกคอร์ท
ค่าลูกรวม           = ราคาต่อลูก × จำนวนลูกที่ใช้ทั้งวัน

ค่าคอร์ทของแต่ละคน = ค่าคอร์ทรวม ÷ จำนวนคนที่มา          ← หารเท่าหมด
ค่าลูกของแต่ละคน   = (ลูกที่ร่วมหารของคนนั้น ÷ ผลรวมลูกที่ร่วมหารของทุกคน) × ค่าลูกรวม

ต้องจ่าย = ปัดขึ้น( ค่าคอร์ทของคนนั้น + ค่าลูกของคนนั้น )
```

**กติกาที่ควรรู้**

- **ปัดขึ้นเฉพาะยอดสุดท้ายต่อคน** ยอดกลางทางไม่ปัด เศษที่เกินจากการปัดจะโชว์เป็น **"ส่วนต่างเข้ากองกลาง"**
- "เวลาเต็มของก๊วน" ที่แสดงใต้ค่าคอร์ท = **คอร์ทที่จองยาวที่สุด** (ไม่ใช่ผลรวมทุกคอร์ท เพราะคอร์ทเล่นพร้อมกัน) — เป็นข้อมูลให้ดูเฉย ๆ ไม่ได้ใช้หาร
- ป้ายราคาที่สร้างไว้แต่ยังไม่ได้แปะลงคอร์ทไหน **จะไม่ถูกคิดเงิน**
- ค่าที่ผิดปกติจะขึ้นเป็น **คำเตือน (⚠️)** ที่หน้าสรุป แต่ไม่บล็อกการคำนวณ
- 💾 บิลที่กรอกค้างไว้ถูกเซฟอัตโนมัติทุกครั้งที่แก้ — สลับแอปไปตอบไลน์แล้วกลับมายังอยู่ครบ

### 2.2 แท็บ ประวัติ

- ตาราง: วันที่เล่น / ชื่อก๊วน / จำนวนคน / ยอดรวม / บันทึกเมื่อ — ใหม่สุดอยู่บน (เรียงตามเวลาที่กดบันทึก ไม่ใช่วันที่เล่น เพราะบิลย้อนหลังกรอกทีหลังได้)
- **คลิกที่แถว** = โหลดบิลนั้นกลับเข้าหน้าคำนวณเพื่อดู/แก้ต่อ
- ปุ่มลบ = ลบบิล (ถามยืนยันก่อน)
- 📌 ประวัติเก็บเป็นข้อมูลดิบ **ยอดถูกคำนวณใหม่ทุกครั้งที่เปิด** — ถ้าสูตรถูกแก้ทีหลัง บิลเก่าจะแสดงยอดตามสูตรใหม่ทันที

### 2.3 แท็บ ตั้งค่า

- **รูป QR พร้อมเพย์** — อัปโหลดครั้งเดียว ใช้ได้ตลอด · รูปถูกย่อก่อนเก็บ และ **เก็บอยู่ในเครื่องเท่านั้น ไม่ได้ส่งขึ้นเซิร์ฟเวอร์**
- **รายชื่อคน** — ชุดเดียวกับ MUDMUE Chok พร้อมบอกว่าแต่ละคน "ติดอยู่กี่บิล" และใครถูกซ่อนอยู่

---

## 3. ⚔️ WuWa MudMue — Ban & Pick

กระดานสำหรับกรรมการ/แคสเตอร์คุมทัวร์นาเมนต์ Wuthering Waves · 4 แท็บ: **Draft / Pool / Timer / History**

### 3.1 แท็บ Draft — กระดานหลัก

**ยังไม่มีแมตช์** → จะเจอฟอร์ม "เริ่มแมตช์ใหม่": ใส่ชื่อ P1 (ซ้าย) / P2 (ขวา)

⚠️ ต้องมีตัวละครในพูล **อย่างน้อย 28 ตัว** ถึงจะกดเริ่มได้ (เท่ากับจำนวนที่ทุกเฟสแบน+เลือกรวมกัน) — ถ้าไม่พอ มีปุ่ม **🎲 โหลดข้อมูลตัวอย่าง** ให้กดเติมได้ทันที

**ลำดับเฟส**

```
Ban 1 → Pick 1 → Ban 2 → Pick 2 → Last Ban → Boss Roll → Battle → Done
```

**ตารางเทิร์น (ลำดับสำคัญ — กระดานบังคับตามนี้ให้เอง)**

| เฟส | ลำดับ | รวม |
|---|---|---|
| **Ban 1** | P1 ×2 → P2 ×2 → P1 ×1 → P2 ×1 | แบนจากพูลกลาง ฝั่งละ 3 |
| **Pick 1** | P1 1 → P2 1 → P1 1 → P2 2 → P1 2 → P2 2 → P1 2 → P2 1 | เลือกฝั่งละ 6 |
| **Ban 2** | P2 → P1 → P2 → P1 (ฝั่งละ 1 สองรอบ) | แบนฝั่งละ 2 · **P2 เริ่ม** |
| **Pick 2** | P2 1 → P1 2 → P2 2 → P1 1 | เลือกเพิ่มฝั่งละ 3 (รวมเป็น 9) · **P2 เริ่ม** |
| **Last Ban** | P2 ×2 → P1 ×2 | **ตัดตัวออกจาก roster ของฝ่ายตรงข้าม** ฝั่งละ 2 → เหลือใช้จริงฝั่งละ 7 |

**Boss Roll** — สุ่มบอสมา **5 ตัว** เรียงเป็นลำดับของแมตช์

**Battle — กติกา**

| กติกา | ค่า |
|---|---|
| ตัวละครแต่ละตัวมีชีวิต | **3** |
| ตีบอส 1 ตัวได้คนละ | **3 ครั้ง** |
| แต้มที่ต้องได้เพื่อชนะแมตช์ | **3** (เคลียร์บอสได้ 1 ตัว = 1 แต้ม) |

- ทุกตัวที่จัดลงตี **เสียชีวิต 1 ดวง ไม่ว่าจะแพ้หรือชนะ**
- **บอสที่ไม่มีใครผ่านทั้งสองฝ่าย → คืนชีวิตให้ทั้งคู่** (ระบบคำนวณชีวิตใหม่จาก log ทั้งหมดทุกครั้ง จึงคืน/หักย้อนหลังได้ถูกเสมอ)
- ชนะบอส = ได้แต้มแล้วไปบอสตัวถัดไป · ถึง 3 แต้มเมื่อไหร่แมตช์จบทันที (เฟสกลายเป็น Done)

**แถบปุ่มด้านบน**

| ปุ่ม | ทำอะไร |
|---|---|
| **↶ Undo** | ย้อนกลับ **1 ขั้น** (เก็บ snapshot ไว้ชั้นเดียว ย้อนซ้อนไม่ได้) |
| **Banned List** | เปิดดูรายชื่อตัวที่ถูกแบนไปแล้วทั้งหมด |
| **Override ON/OFF** | 🔧 **โหมดกรรมการ** — ข้ามกติกาเทิร์นได้ทั้งหมด ใช้ตอนกดพลาดหรือกติกาหน้างานเปลี่ยน |
| **New Match** | ล้างแมตช์ปัจจุบันเริ่มใหม่ (ถามยืนยันก่อน) |

**เมื่อเปิด Override** จะมีแถบเตือนโผล่มาพร้อมตัวเลือก **"บันทึกเป็นของ: P1 / P2"** (ระบุว่าการกดครั้งต่อไปนับเป็นของใคร) และปุ่ม **เทิร์นถัดไป →** / **เฟสถัดไป ⇥** สำหรับเดินเกมเอง เพราะโหมดนี้กระดานจะไม่เดินเทิร์นให้อัตโนมัติ

**ตัวจับเวลา** อยู่มุมขวาบนคู่กับสกอร์ — นับตามเวลาที่ตั้งไว้ของเฟสนั้น ๆ

### 3.2 แท็บ Pool — คลังตัวละคร & บอส

| ปุ่ม | ทำอะไร |
|---|---|
| **+ ตัวละคร** | เพิ่มทีละตัว: ชื่อ, รูป (วาง URL หรืออัปโหลดไฟล์), ธาตุ, ประเภทอาวุธ, ระดับ |
| **+ บอส** | เพิ่มบอส: ชื่อ + รูป |
| **⬇ Export .xlsx** | ดาวน์โหลดพูลทั้งหมด + ประวัติแมตช์ เป็นไฟล์ Excel |
| **⬆ Import .xlsx** | อ่านกลับเข้ามา · แถวที่ไม่มีชื่อจะถูกข้าม · id ที่ชนกันจะถูกสร้างใหม่ให้ · ผลลัพธ์และคำเตือนขึ้นใต้แถบปุ่ม |
| **ไฟล์ตัวอย่าง** | ดาวน์โหลดไฟล์เปล่าที่มีหัวคอลัมน์ถูกต้อง เอาไปกรอกออฟไลน์ |
| **🎲 โหลดข้อมูลตัวอย่าง** | เติมพูล mock ให้ทดลองเล่นได้ทันที |

- **ธาตุ:** Aero · Electro · Fusion · Glacio · Havoc · Spectro
- **อาวุธ:** Sword · Broadblade · Pistols · Gauntlets · Rectifier
- ⚠️ ถ้ามีแมตช์ค้างอยู่จะมีคำเตือน — **การแก้พูลไม่กระทบแมตช์ที่กำลังเล่น** จนกว่าจะเริ่มแมตช์ใหม่

### 3.3 แท็บ Timer

ตั้งเวลา (นาที) แยกของแต่ละเฟส: Ban 1 / Pick 1 / Ban 2 / Pick 2 / Last Ban / Boss Roll และเวลาต่อครั้งของช่วง Battle · กดบันทึกแล้วจะขึ้น "บันทึกแล้ว ✓"

### 3.4 แท็บ History

ตารางแมตช์ที่กด "บันทึกลงประวัติ" ไว้จากหน้าสรุปผล: วันที่ / ผู้เล่น / สกอร์ / ผู้ชนะ / สถานะ / จำนวนครั้งที่ตี / บอสที่แพ้ทั้งคู่

---

## 4. 🗺️ MUDMUE Chim — แผนที่ร้านอาหาร

แผนที่ปักหมุดร้านอาหารพร้อมรีวิว

**วิธีเพิ่มร้าน**

1. กดปุ่ม **"เพิ่มร้านอาหาร"** — จะมีแถบบอกใบ้โผล่กลางแผนที่
2. **คลิกตำแหน่งบนแผนที่**
3. กรอก **ชื่อร้าน (จำเป็น)** / รีวิว-รายละเอียด / คะแนน แล้วบันทึก

**แถบด้านข้าง**

- **ค้นหา** — จากชื่อร้านหรือรายละเอียด
- **เรียงลำดับ** — คะแนน / ระยะทาง / ชื่อร้าน
- **กรองคะแนน** — ทั้งหมด / 3+ / 4+ / 4.5+
- คลิกร้านในลิสต์เพื่อเลื่อนแผนที่ไปหา · แก้ไข/ลบหมุดได้จากหน้าต่างรายละเอียด

> 📍 เว็บจะขอตำแหน่งปัจจุบันเพื่อคำนวณ "ระยะทาง" — ถ้าไม่อนุญาต จะใช้กรุงเทพฯ เป็นจุดตั้งต้นแทน
> ครั้งแรกที่เปิดจะมีข้อมูลร้านตัวอย่างมาให้ พอเพิ่ม/แก้เองแล้วจะถูกแทนที่ด้วยข้อมูลของเรา

---

## 5. 📖 Story Book

หน้ารวม component ของเว็บ (ปุ่มแบบต่าง ๆ ฯลฯ) ไว้ดูว่ามีอะไรให้ใช้บ้างตอนทำหน้าใหม่ — ไม่ใช่ฟีเจอร์สำหรับผู้ใช้ทั่วไป

---

## 💾 ข้อมูลเก็บที่ไหน

**ทั้งเว็บไม่มีเซิร์ฟเวอร์** — ทุกอย่างอยู่ใน localStorage ของเบราว์เซอร์เครื่องนี้เท่านั้น
เปลี่ยนเครื่อง/เปลี่ยนเบราว์เซอร์ = ข้อมูลไม่ตามไป · ล้าง site data = **หายหมด กู้ไม่ได้**

| Key | เก็บอะไร | ของแอป |
|---|---|---|
| `mudmue_profile` | โปรไฟล์ผู้เล่นทั้งหมด + สถิติ win/lose | Chok (+ Han ใช้ร่วม) |
| `mudmue_history` | แมตช์แบดทั้งหมด (ทั้งที่ยังไม่จบและจบแล้ว) | Chok |
| `mudmue_players` ⏳ | ลิสต์ผู้เล่นในหน้า Matchmaker — **sessionStorage หายเมื่อปิดแท็บ** | Chok |
| `mudmue_han_draft` | บิลที่กำลังกรอกค้างอยู่ | Han |
| `mudmue_han_sessions` | บิลที่กดบันทึกแล้ว | Han |
| `mudmue_han_settings` | รูป QR พร้อมเพย์ | Han |
| `mudmue-chim-places` | ร้านอาหารที่ปักหมุดไว้ | Chim |
| `ww_draft_characters` | พูลตัวละคร | WuWa |
| `ww_draft_bosses` | พูลบอส | WuWa |
| `ww_draft_current_match` | แมตช์ที่กำลังเล่น | WuWa |
| `ww_draft_matches` | ประวัติแมตช์ที่บันทึกแล้ว | WuWa |
| `ww_draft_timer_settings` | เวลาต่อเฟส | WuWa |
| `ww_draft_undo` | snapshot สำหรับ Undo 1 ขั้น | WuWa |

**สำรองข้อมูล:** ตอนนี้มีทางเดียวคือ **Export .xlsx ที่แท็บ Pool ของ WuWa** (ได้พูล + ประวัติแมตช์ WuWa) · ส่วน Chok / Han / Chim ยังไม่มีปุ่ม export

---

## 🚧 ยังไม่ได้ทำ / รอ phase 2

- **Chok** — โหมดจับคู่ "ตามฝีมือ" (ยังปิดอยู่) · การแก้ไขแมตช์ย้อนหลังที่ Dashboard/History · คะแนนระหว่างแข่งยังไม่เซฟลงเครื่อง
- **Han** — **ชั่วโมงรายคน** สำหรับคนมาสาย/กลับก่อน (ตอนนี้ค่าคอร์ทหารเท่าหมด โดยถือว่าทุกคนอยู่ครบทั้งก๊วน) · QR ที่ฝังจำนวนเงินไว้เลย · export บิล
- **ทั้งเว็บ** — ยังไม่มี backend ข้อมูลไม่ sync ข้ามเครื่อง

---

<sub>แก้ฟีเจอร์แล้วอย่าลืมอัปเดตไฟล์นี้ด้วย · สเปกละเอียดของ Han อยู่ที่ `public/.claude/mudmue-han-badminton-cost-splitter-plan.md` · ของ WuWa อยู่ที่ `public/.claude/ww-draft-tool-task-plan.md`</sub>
